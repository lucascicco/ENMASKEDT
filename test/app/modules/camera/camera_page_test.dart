import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:enmaskedt/app/modules/camera/camera_page.dart';

void main() {
  testWidgets('CameraPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CameraPage(title: 'Camera')));
    //  final titleFinder = find.text('Camera');
    //  expect(titleFinder, findsOneWidget);
  });
}
